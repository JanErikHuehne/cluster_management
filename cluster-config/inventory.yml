all:
  children:
    ldap_client:
      hosts:
        tuwzc1n-insula:
    slurm_compute:
      hosts: 
        tuwzc1n-insula:
          ansible_host: 10.157.154.7
  vars:
    ansible_user: janhuehne
    ansible_become: true 
    ansible_python_interpreter: /usr/bin/python3 
